#ifndef MAIN_H_
#define MAIN_H_

/**
 * _putchar - prints a single character
 * @c: variable that receives the int value or data type
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int _putchar(char c);

/**
 * print_alphabet - prints alphabet in lower case
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

void print_alphabet(void);

/**
 * print_alphabet_x10 - prints alphabet in lower case
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

void print_alphabet_x10(void);


/**
 * _islower - prints alphabet in lower case
 * @c: receives the int value of the function calling it
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int _islower(int c);


/**
 * _isalpha - prints alphabet in lower case
 * @c: receives the int value of the function calling it
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int _isalpha(int c);

/**
 * print_sign - prints alphabet in lower case
 * @n: receives the int value of the function calling it
 *
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int print_sign(int n);

/**
 * _abs - prints alphabet in lower case
 *
 * Description: prints the int value of the function calling it
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int _abs(int i);

/**
 * _abs - prints alphabet in lower case
 * print_last_digit - nothingness
 * jack_bauer - jacbia
 * times_table - times table
 * add - add integers
 * @: value waiter
 * print_to_98 - prints number
 *print_times_table - times table
 * @n: integer value
 *
 * Description: prints the int value of the function calling it
 *  Return: _putchar returns 0 while print_alphabet returns
 *   nothing because the return type is void
 */

int print_last_digit(int);
void jack_bauer(void);
void times_table(void);
int add(int, int);
void print_to_98(int n);
void print_times_table(int n);

#endif
