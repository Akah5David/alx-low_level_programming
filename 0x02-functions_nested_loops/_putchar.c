#include <stdio.h>
#include "main.h"

/**
 * _putchar - prints a character or a single word at a time
 * @c: variable that receives the character to be printed
 *
 * Return: return an int value 0 at the end of every running of the
 * program.
 */

int _putchar(char c)
{
putchar(c);
return (0);
}
