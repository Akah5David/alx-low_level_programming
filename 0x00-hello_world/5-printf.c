#include <stdio.h>
/**
 * main - prints a statement
 *
 * Declaration: it prints a statement on grammer
 * Return: return 0 always (sucess)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
