#include <stdio.h>
#include "main.h"

/**
 * main - assigns a string to the argument variable of calling
 * funtion
 *
 * Return: returns 0 if successful.
 */

int main(void)
{
_puts_recursion("puts with recursion");
return (0);
}
